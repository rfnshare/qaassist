# Local Agent Override Example

This file is an example only. Do not place secrets, API keys, tokens, customer data, or real credentials here.

Use a temporary local override only when a task needs short-lived local preferences that should not become durable project rules.

Example:

```text
For this local spike only, prefer mock providers and fixture data.
Do not call external services.
```
